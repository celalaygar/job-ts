interface Links {
    dashboard: string;
    home: string;
    login: string;
    register: string;
    profile: string;
    users: string;
    tasks: string;
    sprints: string;
    backlog: string;
}


const links: Links = {
    dashboard: '/dashboard',
    home: '/',
    login: '/',
    register: '/register',
    profile: '/profile',
    users: '/users',
    tasks: '/tasks',
    sprints: '/sprints',
    backlog: '/backlog',
};

export default links; 