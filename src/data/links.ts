interface Links {
    dashboard: string;
    home: string;
    login: string;
    register: string;
    profile: string;
    users: string;
    tasks: string;
    projects: string;
    sprints: string;
    backlog: string;
}


const links: Links = {
    dashboard: '/dashboard',
    home: '/',
    login: '/',
    register: '/register',
    profile: '/profile',
    users: '/users',
    tasks: '/tasks',
    projects: '/projects',
    sprints: '/sprints',
    backlog: '/backlog',
};

export default links; 